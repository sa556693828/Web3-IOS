import React from 'react';
import Animation from '../components/Animation';

const AnimationScreen = ({navigation}) => {
  return <Animation navigation={navigation} />;
};

export default AnimationScreen;
